{
  "name": "setgame-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "test:interactive": "npx playwright test --project=interactive --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
